<h1 class="text-center">Productos</h1>

<div class="grid grid-cols-1 md:grid-cols-3 gap-4 p-4">
  {{#each products}}
    <div class="border rounded-xl p-4 shadow">
      <h3 class="text-xl font-bold">{{title}}</h3>
      <p>{{description}}</p>
      <p><strong>Precio:</strong> ${{price}}</p>
      <p><strong>Categoría:</strong> {{category}}</p>
      <form action="/api/carts/{{../cartId}}/product/{{_id}}" method="POST">
        <button type="submit" class="bg-blue-500 text-white px-3 py-1 rounded mt-2">
          Agregar al carrito
        </button>
      </form>
    </div>
  {{/each}}
</div>

<!-- PAGINACIÓN -->
<div class="flex justify-center gap-4 my-4">
  {{#if hasPrevPage}}
    <a href="/?page={{prevPage}}" class="text-blue-500">⬅ Anterior</a>
  {{/if}}

  <span>Página {{page}} de {{totalPages}}</span>

  {{#if hasNextPage}}
    <a href="/?page={{nextPage}}" class="text-blue-500">Siguiente ➡</a>
  {{/if}}
</div>

<div class="text-center mt-6">
  <a href="/cart/{{cartId}}" class="text-green-600 underline">🛒 Ver mi carrito</a>
</div>